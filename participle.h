struct word
{
	u16string _word;
	u16string _type;
}
bool typeWord()
{
	
}
vector<word> wordSegment(const u16string& line)
{
	vector<word> getWordAndType;
	word._word getWord;
	word._type getType;
	for(auto element:line)
	{
		if()
	}
	
}